# Convex deployment contract. Use separate values for development, preview, and production.
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=
NEXT_PUBLIC_CONVEX_SITE_URL=

# Convex Auth secrets (set with `npx convex env set`, never expose as NEXT_PUBLIC_*).
JWT_PRIVATE_KEY=
JWKS=
SITE_URL=http://localhost:3000
AUTH_RESEND_KEY=
AUTH_EMAIL_FROM=SlotPay PH <auth@example.com>
